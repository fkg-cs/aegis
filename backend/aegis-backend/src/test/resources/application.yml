spring:
  cloud:
    vault:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/aegis_core?sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory
    username: admin
    password: password_segreta_dev

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
